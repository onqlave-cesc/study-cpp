#include <connection/client.h>
#include <requests/requests.h>

int client::Post(std::string resource, const OnqlaveRequest& body,
                 std::map<std::string, std::string> headers) {
  return 0;
}
